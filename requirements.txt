# Pinned runtime + test dependencies (verified on Python 3.14, 2026-09-21).
# Install:  pip install -r requirements.txt
# The scheduled snapshot/pull environment must install this file —
# a bare .venv without these packages is the known cause of past nightly failures.
requests==2.33.0
pandas==3.0.0
numpy==2.4.6
pyarrow==24.0.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
python-dotenv==1.2.2
tqdm==4.67.3
openpyxl==3.1.5
papermill==2.7.0
ipykernel==6.29.5
pytest==9.0.2
